export interface TextElementData {
  id: string;
  text: string;
  x: number; // percentage 0-100
  y: number; // percentage 0-100
  fontSizeVW: number; // e.g., 3 for 3vw
  color: string; // hex string e.g., #RRGGBB
}
